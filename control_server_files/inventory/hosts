# Note-Taking App Inventory - SQLite Version (Single Web Server)
# SQLite is file-based, so database runs on web server

[noteapp_servers]
# Replace with your WEB SERVER IP (EC2 #2)
YOUR_WEB_SERVER_IP ansible_user=ec2-user ansible_ssh_private_key_file=/path/to/your-key.pem

[noteapp_servers:vars]
# Amazon Linux specific configurations
ansible_python_interpreter=/usr/bin/python3
ansible_ssh_common_args='-o StrictHostKeyChecking=no'

# Application variables
app_environment=production
deploy_version=latest

# IMPORTANT: SQLite database will be created on the web server
# Database server (EC2 #3) is not used with SQLite 